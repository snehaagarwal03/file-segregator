GROQ_API_KEY=your_groq_api_key_here

# Optional: override which folder the agent watches (default: ~/Downloads)
# WATCH_FOLDER=/custom/path/to/watch

# Optional: override where category folders are created (default: ~/Downloads)
# e.g. Finance/, Resume/ will appear inside this folder
# CATEGORIES_ROOT=/custom/path/here
